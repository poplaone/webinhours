import WhyChooseAnimated from "@/components/ui/why-choose-animated";

/**
 * Demo component showing how to use WhyChooseAnimated
 * 
 * This component features:
 * - Animated SVG paths with flowing lights
 * - Responsive design optimized for all screen sizes
 * - Feature checklist with icons
 * - Stats cards with hover animations
 * - CTA buttons
 * - Animated circles background
 * 
 * Usage:
 * Simply import and use <WhyChooseAnimated />
 * No props required - all content is pre-configured
 */
export const WhyChooseAnimatedDemo = () => {
  return (
    <div className="w-full">
      <WhyChooseAnimated />
    </div>
  );
};
